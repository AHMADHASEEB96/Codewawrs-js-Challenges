 // You can use return inside if if statement Execution code;
 // couldn't log the same variable to the console after returned it in the function
 // split() splits the content into a new array 
 // again to log a function result in the console. log the invoking code but use return inside the f or log the result itself
/*  if we invoke the function using console.log while the code inside this functions is too logged into the console . 
indeed the code will be logged in the console but still we get undefined because we cant log a code to the console 
 if it is already logged before . the the inner console.log gives the code but the seconde logging gives undefined  */
 // split() creates an array of the splitted content